define(function () {

    // Provide a global location to place configuration settings and module
    // creation.
    var app = {
        // The root path to run the application.
        root: "/backbone-simple-boilerplate/"
    };

    return app; 

});